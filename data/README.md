# /data

This folder is populated by running the data generator from the project root:

    python generate_headcount_data.py

It will produce four files:

- employees.csv
- headcount_monthly.csv
- separations.csv
- new_hires.csv

The raw data files are excluded from version control to keep the repo lightweight. Run the script locally to generate them.
